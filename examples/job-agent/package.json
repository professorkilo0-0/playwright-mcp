{
  "name": "job-agent",
  "version": "1.0.0",
  "description": "AI job search and application agent using Claude + Playwright MCP",
  "type": "module",
  "scripts": {
    "start": "npx tsx agent.ts",
    "search-only": "npx tsx agent.ts --search-only"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.39.0",
    "@modelcontextprotocol/sdk": "^1.10.2",
    "tsx": "^4.19.2"
  }
}
